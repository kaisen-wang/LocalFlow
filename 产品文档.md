设计一款名为 **“LocalFlow (本地流)”** 的PC端纯本地工作待办软件。

针对“工作用”和“纯本地”这两个核心诉求，该软件的设计哲学是：**极致轻量、键盘优先、绝对隐私、零延迟**。摒弃一切花哨的云端社交和协作功能，专注于提升个人的工作心流。

以下是详细的产品设计方案：

---

### 一、 产品定位与核心特性

*   **定位**：面向程序员、文字工作者、设计师等需要深度专注的PC端用户的离线任务管理工具。
*   **核心特性**：
    1.  **纯本地运行**：不请求任何网络权限，数据100%留在本地硬盘。
    2.  **毫秒级响应**：无网络加载延迟，启动和交互如丝般顺滑。
    3.  **键盘流操作**：支持全键盘快捷键，双手不离开键盘即可完成90%的操作。
    4.  **数据主权**：提供本地自动备份和多种格式导出，数据绝不绑架用户。

---

### 二、 核心功能模块设计

#### 1. 快速收集 (Quick Capture) - *灵魂功能*
*   **全局快捷键**：默认 `Ctrl+Shift+Space` (或 `Alt+Space`) 呼出悬浮输入框。
*   **自然语言解析**：输入“明天下午3点交报告 #工作 !高”，自动识别为：截止时间（明天15:00）、标签（工作）、优先级（高）。
*   **一键暂存**：按 `Enter` 直接存入“收集箱(Inbox)”，不打断当前工作流。

#### 2. 任务管理 (Task Management)
*   **智能视图**：
    *   **今日 (Today)**：只显示今天需要完成和已逾期的任务。
    *   **计划 (Upcoming)**：按时间轴展示未来7天的任务。
    *   **随时 (Someday)**：没有明确截止日期的灵感或长线任务。
    *   **收集箱 (Inbox)**：未分类、未整理的快速记录。
*   **任务详情**：支持富文本/Markdown编辑，支持添加**子任务 (Checklist)**，支持添加本地附件（如截图、文档，以相对路径或Base64存储在本地）。

#### 3. 工作流辅助 (Workflow Tools)
*   **内置番茄钟**：选中任务后，一键开启25分钟专注倒计时，界面进入极简沉浸模式。
*   **项目/文件夹**：支持按工作项目（如“Q3产品发布”、“日常运维”）对任务进行物理隔离。
*   **看板视图 (Kanban)**：提供 待办 -> 进行中 -> 已完成 的拖拽看板，适合敏捷工作管理。

---

### 三、 UI/UX 与交互设计

#### 1. 界面布局 (经典三栏式，支持折叠)
*   **左侧导航栏**：视图切换（今日、计划等）、项目/文件夹列表、标签管理。
*   **中间任务列表**：清晰的任务条目，左侧复选框，右侧显示截止日期和优先级色块。
*   **右侧详情面板**：点击任务后滑出，显示详细描述、子任务、附件和历史记录。（屏幕较小时可改为弹窗或抽屉）。

#### 2. 视觉与主题
*   **极简设计**：去除多余边框和阴影，采用大留白。
*   **护眼主题**：内置“深色模式”、“浅色模式”以及“护眼绿/纸张黄”模式，自动跟随系统或定时切换。
*   **状态反馈**：完成任务时，提供轻微的震动反馈（Windows）或清脆的“咔哒”音效（可关闭），并给予一个微小的撒花或划线动画。

#### 3. 交互细节
*   **系统托盘驻留**：关闭窗口后最小化到系统托盘，不占用任务栏。
*   **开机自启**：支持静默开机启动，随叫随到。

---

### 四、 技术架构与本地存储方案

为了保证“纯本地”和“高性能”，技术选型建议如下：

*   **客户端框架**：
    *   *首选*：**Tauri** (Rust + Web前端)。比Electron轻量10倍以上，内存占用极小，非常适合做本地工具。
    *   *备选*：**C# WPF / WinUI 3** (如果仅针对Windows) 或 **Qt** (跨平台C++)。
*   **本地数据库**：
    *   **SQLite**：单文件数据库，适合存储任务、项目、设置等结构化数据，查询速度快。
*   **文件存储**：
    *   附件和图片存储在软件安装目录或用户文档目录下的 `LocalFlow_Data/assets/` 文件夹中。

---

### 五、 数据安全与备份机制（纯本地的核心痛点）

纯本地软件最大的风险是“硬盘损坏导致数据丢失”，必须通过以下机制解决：

1.  **自动本地备份**：
    *   软件每次关闭时，或每天首次打开时，自动将 SQLite 数据库和附件目录打包成一个 `.zip` 或 `.bak` 文件。
    *   备份路径允许用户自定义（例如备份到个人的移动硬盘或NAS的本地映射盘）。
    *   保留最近 7 天的滚动备份，防止误操作覆盖。
2.  **纯文本导出 (防软件倒闭/格式锁定)**：
    *   支持一键将所有数据导出为 **Markdown** 文件（每个任务一个md文件，或按项目合并）或 **JSON/CSV**。
    *   确保即使未来不用这个软件了，数据也能被其他软件（如Obsidian, Notion）轻松读取。
3.  **数据加密 (可选高级功能)**：
    *   提供“数据库加密”选项，使用 AES-256 对本地 SQLite 文件加密，防止电脑借给他人时隐私泄露。

---

### 六、 开发与落地路线图

如果您打算自己开发或找团队开发，建议分阶段进行：

*   **Phase 1 (MVP 最小可行性产品 - 2周)**：
    *   实现全局快捷键快速收集。
    *   实现基础的增删改查和今日/收集箱视图。
    *   完成 SQLite 本地存储和基础 UI。
*   **Phase 2 (核心工作流 - 3周)**：
    *   加入项目/标签管理。
    *   实现自然语言解析（日期、优先级）。
    *   加入任务详情和 Markdown 支持。
*   **Phase 3 (效率与安全 - 2周)**：
    *   加入内置番茄钟。
    *   完善自动本地备份机制和导出功能。
    *   优化键盘快捷键和系统托盘交互。
*   **Phase 4 (打磨与发布)**：
    *   跨平台构建与验收清单。
    *   性能、品牌图标、安装包发布流程与用户手册。

### 总结

这款 **“LocalFlow”** 不追求大而全，而是将 **“本地化”** 和 **“工作专注”** 做到极致。它没有云端同步的焦虑，没有账号注册的繁琐，打开即用，数据绝对安全。对于需要深度工作的PC用户来说，这样一款克制、高效的本地工具，往往比功能臃肿的SaaS产品更能提升生产力。
