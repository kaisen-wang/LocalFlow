# LocalFlow 用户手册

面向日常使用：怎么装、怎么用、数据在哪、出问题怎么办。

更详细的功能清单见 [`README.md`](./README.md)，产品设计见 [`产品文档.md`](./产品文档.md)。

---

## 1. 它是什么

LocalFlow（本地流）是电脑上的**待办工具**：

- **不联网**、不注册账号
- 数据只存在你自己的电脑里
- 尽量用键盘就能操作

---

## 2. 怎么安装

### 方式 A：下载安装包（推荐）

1. 打开 GitHub 仓库的 **Releases（发布）** 页面
2. 按你的系统下载：
   - **Windows**：`.msi` 或 `.exe`
   - **macOS**：`.dmg`（Apple 芯片选 `aarch64`，Intel 选 `x64`）
   - **Linux**：`.AppImage` 或 `.deb`
3. 安装/打开后即可使用

### 方式 B：自己从源码运行（开发者）

需要先装好 Node.js、Rust，以及 [Tauri 系统依赖](https://v2.tauri.app/start/prerequisites/)。

```sh
npm install
npm run tauri dev      # 开发调试
npm run tauri build    # 打正式包
```

打正式包前可先跑自检：

```sh
npm run release:check
```

---

## 3. 日常怎么用（最常用）

| 你想做的事 | 怎么做 |
|------------|--------|
| 快速记一条 | `Ctrl+Shift+Space`，输入后 Enter |
| 新建任务 | 主窗口顶部输入框，Enter；可用 `明天` `#标签` `!高` |
| 完成任务 | 点复选框，或选中后按 `x` / 空格 |
| 看今天的事 | 左侧「今日」或 `Ctrl+2` |
| 整理项目 | 左侧「项目」点 `+`；双击改名 |
| 专注 25 分钟 | 选中任务后按 `p`，或点「番茄钟」 |
| 看板拖拽 | 左侧「看板」或 `Ctrl+5` |
| 备份数据 | 左侧「数据」→「立即备份」 |
| 看快捷键 | 按 `?` |

完整快捷键表见 README「常用快捷键」。

---

## 4. 主题与设置

左侧 **设置**：

- **主题**：浅色 / 深色 / 护眼绿 / 纸张黄 / 跟随系统（自定义下拉）
- **开机自启**：勾选后登录系统自动启动
- **完成音效**：勾选完成任务时的「咔哒」声
- **数据库加密**：可选；开启后每次打开要输入密码（至少 6 位）

任务详情里的**截止日期**用自定义日历（可点「今天 / 清除」），**优先级 / 所属项目**用自定义下拉，外观会跟当前主题一致。

关闭主窗口会**藏到系统托盘**（没有真正退出）。要退出：托盘图标右键 →「退出 LocalFlow」。

---

## 5. 数据存在哪里

| 内容 | 位置 |
|------|------|
| 主数据库 | 应用数据目录下的 `localflow.db` |
| 是否加密 | 同目录 `encryption.json`（只记开关，**不存密码**） |
| 滚动备份 | 同目录 `backups/`（默认保留最近 7 份） |

常见应用数据目录：

- **Linux**：`~/.local/share/com.carson.localflow/`
- **macOS**：`~/Library/Application Support/com.carson.localflow/`
- **Windows**：`%APPDATA%\com.carson.localflow\`

导出：侧栏「数据」可导出 Markdown / JSON / CSV，方便迁到其他软件。

---

## 6. 加密要注意

1. 密码**不会上传**，也不会写进文件  
2. **忘记密码 = 打不开已加密的数据**，请自己妥善保存  
3. 开启加密后，备份文件也是加密的  

---

## 7. 各系统注意点

| 系统 | 注意 |
|------|------|
| Linux | 托盘需要桌面支持 StatusNotifier / AppIndicator；全局快捷键可能被其他软件占用 |
| macOS | 首次可能提示「无法验证开发者」，需在系统设置里允许；全局快捷键需辅助功能权限 |
| Windows | 杀毒软件偶发误报本地工具，可加入白名单；托盘在通知区域 |

更完整的人工验收项见 [`跨平台测试清单.md`](./跨平台测试清单.md)。

---

## 8. 出问题了怎么办

1. **打不开 / 闪退**：看是否开了加密却密码不对；可从 `backups/` 拷回最近备份再试  
2. **快捷键没反应**：检查是否被系统或其他软件占用；输入框聚焦时部分快捷键故意不抢键  
3. **托盘找不到**：Linux 换支持托盘的桌面，或从启动器再开一次应用  
4. **数据想搬家**：复制整个应用数据目录，或用导出功能  

---

## 9. 版本与更新

当前版本号见 `package.json` / `src-tauri/tauri.conf.json`（应保持一致）。

发布流程（给维护者）：

1. 改版本号（两处 + 如有需要文档）  
2. `npm run release:check`  
3. 打 tag：`git tag v1.0.0 && git push origin v1.0.0`  
4. GitHub Actions「Release」会打出各平台安装包（草稿 Release）  
